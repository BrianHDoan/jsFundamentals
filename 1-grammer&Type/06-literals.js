// * Literals

// * JavaScript Syntax defines two types pf values: Fixed and variable values.
// * Fixed values are called literals.
// * Variables values are called variables.

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday','Friday'];
console.log(days.toString());
console.log(days.length);

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley'
};
console.log(soup.b);
console.log(soup.c);

let car = 'Honda';

let dec = 10.2;